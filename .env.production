# Production webview build (packaged Tauri app). No Vite proxy exists in the
# packaged build, so the webview calls the Node sidecar's loopback URL directly.
# Loaded automatically by `vite build`. See docs/packaging-runbook.md.
VITE_API_BASE=http://127.0.0.1:5179/api
