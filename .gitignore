# Dependencies
node_modules/
reference-implementation/node_modules/

# Build output
dist/
dist-ui/
dist-host/
coverage/

# Tauri (Rust build + generated, sidecar binaries)
src-tauri/target/
src-tauri/gen/
src-tauri/binaries/

# Local database & Prisma local artifacts
*.db
*.db-journal
dev.db
prisma/dev.db

# Environment / secrets
.env
.env.*
!.env.example
# Production webview build config (localhost URL, no secret) — tracked.
!.env.production

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.log

# Tauri (later phases)
src-tauri/target/

# Mobile icon sets (desktop build only)
src-tauri/icons/ios/
src-tauri/icons/android/
